# Uploads Directory

This directory stores user-uploaded files and form submissions.

## What gets stored here:
- Files uploaded through `/upload.html`
- Form submissions in `submissions.txt`

## Note
This directory is ignored by git (except this README), so each user gets a fresh start.
